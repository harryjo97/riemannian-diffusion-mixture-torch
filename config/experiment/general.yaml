# @package _global_

name      : general
experiment: general
torus_dim : 2
batch_size: 4096
steps     : 500000

defaults:
  - protein
  - /dataset: general

beta_schedule:
  beta_0: 0.1
  beta_f: 0.1

model:
  num_layers: 4
  hidden_dim: 512

optim:
  lr: 1e-4