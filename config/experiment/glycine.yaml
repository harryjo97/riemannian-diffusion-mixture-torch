# @package _global_

name      : glycine
experiment: glycine
torus_dim : 2
batch_size: 4096
steps     : 500000

defaults:
  - protein
  - /dataset: glycine

beta_schedule:
  beta_0: 0.01
  beta_f: 0.01

model:
  num_layers: 5
  hidden_dim: 512

optim:
  lr: 4e-4